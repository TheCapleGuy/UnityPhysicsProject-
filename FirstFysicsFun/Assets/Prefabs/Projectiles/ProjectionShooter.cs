using UnityEngine;
using System.Collections;

public class ProjectileShooter : MonoBehaviour
{

	// Use this for initialization
	void Start ()
    {
	    
	}
	
	// Update is called once per frame
	void Update ()
    {
        //spawn prefab
        // either 3 prefabs all with different directional velocity
        //^^ if so make direction on projectile launch c# a public variable 
        //else 
        // make prefab of 3 smaller projection rocks.

        //spawns 3 prefabs all x time apart and dissapears(destroys).
        // get smoke animation for destroyed objects.
    }
}
